/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental: {
        serverActions: true,
    },
    images: {
        domains: ['fiudxhjhibxzprgtwfvp.supabase.co'],
    },
}

module.exports = nextConfig
